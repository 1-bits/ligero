# Travis CI Configuration File

# Tell Travis CI we're using PHP
language: php

# Build matrix options
matrix:
  include:
    - php: 5.5
      env: WP_TRAVISCI=travis:js
    - php: 5.3
      env: WP_TRAVISCI=travis:phpunit
    - php: 5.4
      env: WP_TRAVISCI=travis:phpunit
    - php: 5.5
      env: WP_TRAVISCI=travis:phpunit
    - php: 5.6
      env: WP_TRAVISCI=travis:phpunit
    - php: 7
      env: WP_TRAVISCI=travis:phpunit
    - php: hhvm
      env: WP_TRAVISCI=travis:phpunit
    
  allow_failures:
    - php: 5.6
    - php: hhvm
  fast_finish: true

env:
  - WP_VERSION=master WP_MULTISITE=1

script: "phpunit -c ./test/phpunit.xml.dist"
#language: php
#php:
#  - '5.4'
#  - '5.5'
#  - '5.6'
#  - '7.0'
#  - hhvm
#  - nightly
